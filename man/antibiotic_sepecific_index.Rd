% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abx_index.R
\name{antibiotic_sepecific_index}
\alias{antibiotic_sepecific_index}
\alias{vancomycin_index}
\alias{tetracycline_index}
\alias{penicillin_index}
\alias{gram_positive_index}
\alias{gram_negative_index}
\alias{anaerobes_index}
\alias{aerobes_index}
\alias{aminoglycoside_index}
\title{Calculate antibiotic-specific index values}
\usage{
vancomycin_index(
  abundance,
  lineage,
  replace_zero = 1e-04,
  antibiotic_db = taxon_susceptibility,
  phenotype_db = taxon_phenotypes
)

tetracycline_index(
  abundance,
  lineage,
  replace_zero = 1e-04,
  antibiotic_db = taxon_susceptibility
)

penicillin_index(
  abundance,
  lineage,
  replace_zero = 1e-04,
  antibiotic_db = taxon_susceptibility
)

gram_positive_index(
  abundance,
  lineage,
  replace_zero = 1e-04,
  phenotype_db = taxon_phenotypes
)

gram_negative_index(
  abundance,
  lineage,
  replace_zero = 1e-04,
  phenotype_db = taxon_phenotypes
)

anaerobes_index(
  abundance,
  lineage,
  replace_zero = 1e-04,
  phenotype_db = taxon_phenotypes
)

aerobes_index(
  abundance,
  lineage,
  replace_zero = 1e-04,
  phenotype_db = taxon_phenotypes
)

aminoglycoside_index(
  abundance,
  lineage,
  replace_zero = 1e-04,
  antibiotic_db = taxon_susceptibility,
  phenotype_db = taxon_phenotypes
)
}
\arguments{
\item{abundance}{A vector of taxon abundances in a sample}

\item{lineage}{A character vector of taxonomic assignments or lineages}

\item{replace_zero}{Zero-replacement value. If the numerator or denominator
is smaller than this value, they will be replaced with the number here.
This number should generally be slightly less than the minimum measurement
that could be acquired with the method used. For count data, a value of
0.5 is typical. For relative abundances, a number that is slightly lower
than the lowest relative abundance will work.}

\item{antibiotic_db}{A data frame with columns named "taxon", "rank",
"antibiotic", and "value"}

\item{phenotype_db}{A data frame with columns named "taxon", "rank",
"gram_stain", and "aerobic_status"}
}
\description{
Calculate antibiotic-specific index values
}
\examples{
h22 <- weiss2021_data[weiss2021_data$sample_id \%in\% "Healthy.22",]
vancomycin_index(h22$proportion, h22$lineage)
}
